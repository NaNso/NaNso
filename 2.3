#include <stdio.h>
#include <math.h>

int main(void)
{
int a, b, c, d, g, h;
 a = 5;
 b = 3;
 c = 7;
 d = b*b - 4*a*c
  if(d > 0)
  {
  g = (-b + sqrtf(d)) / 2*a;
  n = (-b - sqrtf(d)) / 2*a;
  }
  else if (d == 0)
  {
  g = -b/ 2*a
  }
  else if (d < 0)
  {
  printf("error");
  }
}
